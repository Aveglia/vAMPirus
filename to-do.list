Things to work on for the future:

1. Change workingdir to projectDir

2. Change vampdir to launchDir

3. Test if we can add "if" statement to "label" section
rm tmp_line.list tmpline2.list ORFs_on_rdrp_scafs.csv name.list
